import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Register = () => {

    return (
        <div>
            <h1>registser </h1>
        </div>
    );
};

export default Register;